import React from 'react';
import CommonLayout from '../../CommonLayout/index';

interface Props {}

const HomePresenter = (props: Props) => {
  return <CommonLayout>HomePresenter</CommonLayout>;
};

export default HomePresenter;
