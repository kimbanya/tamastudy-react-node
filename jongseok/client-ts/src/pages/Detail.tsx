import React from 'react';
import CommonLayout from '../components/CommonLayout/index';

interface Props {}

const Detail = (props: Props) => {
  return <CommonLayout noFooter>Detail</CommonLayout>;
};

export default Detail;
